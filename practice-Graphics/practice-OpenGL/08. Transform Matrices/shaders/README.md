# Shaders

Policy: GLSL shader sources for this sample live in this folder.

Current status: Implemented. The scaffold keeps a placeholder `.glsl` file so the Visual Studio project and repository layout stay consistent. Once a sample uses programmable pipeline features, keep vertex, fragment, geometry, or compute shader source here unless the README states a different policy.

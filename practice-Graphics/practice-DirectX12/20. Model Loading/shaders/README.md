# Shaders

Policy: external HLSL source copied to the output directory.

`Model Loading.hlsl` shades the vertices loaded from the external OBJ mesh. The shader is intentionally simple so the sample remains focused on mesh file parsing and vertex/index buffer binding.

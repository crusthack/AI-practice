# Assets

Policy: external mesh asset copied to the output directory.

`sample_model.obj` is a tiny deterministic OBJ mesh used by the sample. The loader supports `v` position records and triangular `f` records, which is enough to demonstrate the asset-to-buffer path without introducing a full model import library.

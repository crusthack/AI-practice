# Shaders

Policy: external HLSL source.

`HelloTriangle.hlsl` contains the vertex and pixel shader entry points used by this sample:

- `VSMain`: transforms already-normalized clip-space triangle positions into `SV_POSITION`.
- `PSMain`: returns the interpolated vertex color.
